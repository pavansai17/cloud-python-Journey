#Game is simple.Here,we store a secret word inside our program and the user guesses it
secret_word = "github"
guess = ""
while guess != secret_word:
    guess = input("Enter guess:")

print("You win!")
